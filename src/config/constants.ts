/**
 * The default page size
 */
export const defaultPageSize = 9;

/**
 * The storage key for active language fetch from use-intl, or we can customize
 * activeLanguage via appolo query context[cookieActiveLocale] = ''
 */
export const cookieActiveLocale = 'NEXT_LOCALE';

/**
 * The storage key for active channel
 */
export const storageActiveChannelKey = 'activeChannel';

/**
 * The cookie key for active channel
 */
export const cookieAuthTokenKey = 'authToken';
